<Page
    x:Class="RegistrationAndUpdateApplicationUWP.Views.BorrowedItemListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RegistrationAndUpdateApplicationUWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="#FF013750">

    <Grid Background="#FFFEF5C8" MaxHeight="700" MaxWidth="1140" CornerRadius="10">
        <TextBlock Padding ="0,10" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="20,0,0,0" Text="Lisa de Objetos Emprestados" FontSize="20" FontFamily="Microsoft YaHei" Foreground="#FF2C6B74" MaxWidth="229" TextWrapping="Wrap" TextAlignment="Center" FontWeight="Bold"/>

        <ListView Background="Transparent" BorderBrush="Transparent" Margin="0,70,0,0" Height="500" Width="800" >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid MaxHeight="200">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="450"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Width="150" Height="100">
                            <Canvas Height="30" Width="50">
                                <Canvas.Background>
                                    <ImageBrush ImageSource="/Assets/itens (2).png" Stretch="Uniform"/>
                                </Canvas.Background>
                            </Canvas>
                        </Border>

                        <StackPanel Grid.Column="1">
                            <TextBlock Text="{Binding Name}" FontSize="20" FontWeight="SemiBold" HorizontalAlignment="Left" Margin="0,0,0,6" LineHeight="10"/>
                            <TextBlock Margin="0,0,0,0" Grid.Column="2" Text="{Binding Description}" TextAlignment="Justify" FontFamily="Segoe UI" FontWeight="Normal" TextWrapping="Wrap" FontSize="16"/>

                            <StackPanel Orientation="Horizontal">
                                <TextBlock  Text="{Binding LoanDate}" FontSize="12" Margin="0,5,0,0" FontWeight="SemiBold" HorizontalAlignment="Left"/>
                                <TextBlock  Text=" a " FontSize="12" Margin="0,5,0,0" FontWeight="SemiBold" HorizontalAlignment="Left"/>
                                <TextBlock  Text="{Binding ReturnDate}" FontSize="12" Margin="0,5,0,0" FontWeight="SemiBold" HorizontalAlignment="Left"/>
                            </StackPanel>
                        </StackPanel>

                        <TextBlock Grid.Column="2" Text="{Binding RegistrationDate}" VerticalAlignment="Bottom" Margin="20,0,0,0"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
