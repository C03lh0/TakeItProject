<Page
    x:Class="TakeIt.UWP.Views.BorrowedItemFormView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TakeIt.UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:converters="using:TakeIt.UWP.Converters" 
    xmlns:storage="using:Windows.Storage"
    mc:Ignorable="d" Background="#FF013750">

    <Page.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="FontSize" Value="18"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="Label">
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei"/>
            <Setter Property="Foreground" Value="Black"/>
        </Style>

        <converters:StorageFileImageConverter x:Key="StorageFileImageConverter"/>
        <DataTemplate x:Key="ThumbinailBorrowedItem" x:DataType="storage:StorageFile">
            <Image Height="250" Width="250" Stretch="Uniform" Source="{x:Bind Converter={StaticResource StorageFileImageConverter}}"/>
        </DataTemplate>

    </Page.Resources>

    <Grid HorizontalAlignment="Center"  MaxHeight="700" Width="1150" Padding="10" Background="#fef5c8" CornerRadius="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Registrar/Atualizar Objeto Emprestado" HorizontalAlignment="Left" Width="250" Style="{StaticResource TextLogo}"/>
        <Grid Grid.Row="1" Margin="0,20,0,0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" >
                <TextBlock TextWrapping="Wrap" MaxWidth="300" TextAlignment="Center" Text="{Binding Name, Mode=OneWay}" Foreground="Black" FontFamily="Arial Black" FontSize="25" FontWeight="Bold" HorizontalAlignment="Center"/>
                <GridView IsItemClickEnabled="True" ItemClick="{x:Bind ViewModel.AddImage}" HorizontalAlignment="Center" MaxWidth="400" ItemsSource="{x:Bind ViewModel.CurrentImage}" ItemTemplate="{StaticResource ThumbinailBorrowedItem}"/>

                <StackPanel Orientation="Horizontal" Width="500">
                    <Button Style="{StaticResource MainButton}" Content="Adicionar imagem" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,30,0,0" CornerRadius="6" Click="{x:Bind ViewModel.AddImage}"/>
                    <TextBlock Margin="170,0,0,0" HorizontalAlignment="Right" VerticalAlignment="Bottom" TextAlignment="Right" Foreground="Black">
                        <Run Text="Restam "/>
                        <Run Text="{x:Bind ViewModel.DaysLeftForReturn}"/>
                        <Run Text=" para a devolução."/>
                    </TextBlock>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="0,0,50,0" MaxWidth="400">
                <TextBlock Text="Adicione um novo item" FontFamily="Microsoft YaHei" Foreground="Black" FontSize="30" Margin="0, 0, 0, 20" FontWeight="Bold"/>

                <TextBlock Text="Nome do Objeto" Style="{StaticResource Label}"/>
                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>

                <TextBlock Text="Data de Empréstimo" Style="{StaticResource Label}"/>
                <DatePicker SelectedDate="{Binding LoanDate, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Margin="0,0,0,10"/>

                <TextBlock Text="Data de Devolução" Style="{StaticResource Label}"/>
                <DatePicker SelectedDate="{Binding ReturnDate, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Margin="0,0,0,10"/>

                <TextBlock Text="Descrição" Style="{StaticResource Label}"/>
                <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" TextWrapping="Wrap" Height="150"/>
            </StackPanel>
        </Grid>
        
        <StackPanel Grid.Row="2" Margin="302,0,0,-6" VerticalAlignment="Bottom" HorizontalAlignment="Left" Orientation="Horizontal">
            <Button Style="{StaticResource MainButton}" Margin="0,0,100,0" Foreground="White" Content="Registrar" MaxWidth="200" CornerRadius="6" Click="{x:Bind ViewModel.Registrate}"/>
            <Button Style="{StaticResource MainButton}" Margin="0,0,100,0" Foreground="White" Content="Atualizar" MaxWidth="200" CornerRadius="6" Click="{x:Bind ViewModel.Update}" Visibility="Visible"/>
            <Button Style="{StaticResource MainButton}" Margin="0,0,100,0" Foreground="White" Content="Deletar" MaxWidth="200" CornerRadius="6" Click="{x:Bind ViewModel.Delete}"/>
            <Button Style="{StaticResource MainButton}" Margin="0,0,0,0" Foreground="White" Content="Cancelar" MaxWidth="200" CornerRadius="6" Click="{x:Bind ViewModel.Cancel}"/>
        </StackPanel>
        
    </Grid>
</Page>
